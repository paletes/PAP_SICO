import { StyleSheet, View, Image, SafeAreaView, TouchableOpacity } from 'react-native';
import React, { Component } from "react";
import { Container, Header, Title, Left, Right, Body, Text } from "native-base";
import styles from "../styles/baseStyles";
import { faAlignCenter } from '@fortawesome/free-solid-svg-icons';

export function Camara({ navigation }) {
          return (
            <SafeAreaView style={styles.PaginaInicial}>
            <Text style={styles.PaginaInicial_title1}>Nova Fatura</Text>
            
        </SafeAreaView>
          );
    };
   

